import React from 'react';

const TutorialPlayer = ({ streamUrl }) => {
  return (
    <div>
      <h3>Tutorial Player (Placeholder)</h3>
      <p>Stream URL: {streamUrl}</p>
    </div>
  );
};

export default TutorialPlayer;